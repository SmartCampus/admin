{% if (tag == NULL) or (tag == "indiferent") %}
<div class="well-sm"> 
    <form id="my_form" action="{{ path('smartcampus_redirect') }}" method="post">
        Type :
        <select id="kind" name="kind">
            <option value="indiferent">indiferent</option>
            <option value="temperature">temperature</option>
            <option value="luminosite">light</option>
        </select>
        <input type="submit" value="Filtrer">
    </form>
</div>
{% endif %}

{% if tag == "temperature" %}
<div class="well-sm"> 
    <form id="my_form" action="{{ path('smartcampus_redirect') }}" method="post">
        Type :
        <select id="kind" name="kind">
            <option value="indiferent">indiferent</option>
            <option selected value="temperature">temperature</option>
            <option value="luminosite">light</option>
        </select>
        <input type="submit" value="Filtrer">
    </form>
</div>
{% endif %}

{% if tag == "luminosite" %}
<div class="well-sm"> 
    <form id="my_form" action="{{ path('smartcampus_redirect') }}" method="post">
        Type :
        <select id="kind" name="kind">
            <option value="indiferent">indiferent</option>
            <option value="temperature">temperature</option>
            <option selected value="luminosite">light</option>
        </select>
        <input type="submit" value="Filtrer">
    </form>
</div>
{% endif %}

{% if tag == "presence" %}
<div class="well-sm"> 
    <form id="my_form" action="{{ path('smartcampus_redirect') }}" method="post">
        Type :
        <select id="kind" name="kind">
            <option value="indiferent">indiferent</option>
            <option value="temperature">temperature</option>
            <option value="luminosite">light</option>
        </select>
        <input type="submit" value="Filtrer">
    </form>
</div>
{% endif %}