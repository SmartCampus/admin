{% extends "SmartCampusBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Index{% endblock %}

{% block body %}
    <h1>{{ titre }}</h1>

    
    <div class="container">
        
        <!-- DataTable -->
        {% include "SmartCampusBundle:Smart:datatable.html.twig" %}

        <a href="{{ path('smartcampus_ajouterB') }}" class="btn btn-primary bouton-accueil">
            + Board
        </a>
        <a href="{{ path('smartcampus_ajouterV') }}" class="btn btn-primary bouton-accueil">
            + Capteur Virtuel
        </a>
        <a href="{{ path('smartcampus_ajouterP') }}" class="btn btn-primary bouton-accueil">
            + Capteur Physique
        </a>
    </div>
{% endblock %}